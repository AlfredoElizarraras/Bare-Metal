<h1>Edit user </h1>

<%= form_for @user do |f| %>
   <%= f.label :username, value: 'User name: ' %>
   <%= f.text_field :username, placeholder: 'Carlos' %>
   <%= f.label :email, value: 'E-mail' %>
   <%= f.text_field :email, placeholder: 'example@mail.com' %>
   <%= f.submit "Submit" %>
 <% end %>